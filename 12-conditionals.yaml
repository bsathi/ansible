- name: conditionals
  hosts: local
  connection: local
  vars:
    number: 12  # try with 6 as well
  tasks:
  - name: check number is less than 10 or not
    ansible.builtin.debug:
      msg: "Given number {{ number }} is less than 10"
    when: number < 10